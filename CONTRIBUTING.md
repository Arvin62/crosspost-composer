# 贡献指南

欢迎通过 GitHub Issues 反馈问题、兼容性表现和主题建议。

请优先使用仓库提供的结构化模板：

- Bug 报告必须包含版本、浏览器、操作系统和不含隐私数据的最小复现；
- 平台兼容性报告必须记录目标平台、验证日期、输出路径和预览结果；
- 新功能建议需要说明当前重复劳动、可验证结果，以及是否引入网络、凭证或自动化权限；
- 安全问题必须按照 [SECURITY.md](SECURITY.md) 私下提交。

不要公开未发布稿件、完整备份、Cookie、Token、API Key 或个人信息。

为保持版权归属以及个人免费 / 商业授权边界清晰，在贡献者协议准备完成前，项目暂不接收外部代码 Pull Request。请不要直接提交包含代码实现的 PR；可以先在 Issue 中描述问题、复现步骤或设计建议。

## 开发

```bash
npm install
npm run dev        # http://localhost:5173
npm run typecheck  # 类型检查
npm test           # 自动化测试
npm run build      # 构建
npm run verify     # 测试 + 类型检查 + 构建
npm run audit:ci   # 中危、高危与严重依赖漏洞检查
npm run security:secrets # 检查待提交文件中的高置信度凭证特征
```

## 本地新增一套排版主题

1. 打开 `src/themes/presets.ts`，仿照已有主题写一个 `Theme` 对象：键是标签选择器（`h2` / `p` / `blockquote` …），值是 CSS 声明串。
2. 加进底部的 `THEMES` 数组即可，主题下拉会自动出现。
3. **注意**：只用「可内联」的属性，不要用 `::before` 等伪元素——因为样式最终会被内联进元素的 `style` 属性以便在各平台存活。字体请走系统字体栈，避免依赖平台不装的字体。

## 代码风格

- TypeScript，开启 `strict`；提交前依次跑 `npm test`、`npm run typecheck` 和 `npm run build`。
- `core/` 保持框架无关、不碰具体 UI（为将来的浏览器插件 / 桌面版复用）；UI 相关放 `ui/`。
- 注释解释「为什么」，不写「做了什么」。
- 改动导入、恢复、网络资源、剪贴板、导出或数据删除逻辑时，必须增加覆盖相应安全边界的测试，并核对[威胁模型](docs/security/threat-model.md)。
- 不得提交 `.env`、私钥、访问令牌、真实备份、未发布稿件或 `.pro-workspace/` 内容；自动扫描只是最后一道防线，不能代替提交前检查。

## 贡献代码

- 外部代码贡献通道目前暂停，等待贡献者协议和版权授权流程完成。
- 未经书面确认提交的代码，不代表项目方已经接受、合并或取得商业再授权权利。
- 如果未来重新开放代码贡献，每个 PR 仍将要求聚焦单一事项，并说明复制、插图、导入等核心行为在哪些平台验证过。

当前许可证转换条件和仍需维护者确认的权属事项，记录在[开源准备审计](docs/governance/open-source-readiness.md)中。
